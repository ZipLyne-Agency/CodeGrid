/**
 * Legacy premium-link hook. Wallet linking is disconnected; no-op.
 */
export function useEntitlement() {
  // no-op — every feature is free
}

export async function startPremiumLink() {
  // no-op
}
