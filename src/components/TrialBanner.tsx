/**
 * TrialBanner — open-source build.
 *
 * CodeGrid is fully open source with no trial or paid tier, so there is no
 * banner to show. This renders nothing; it exists only to satisfy the import
 * left behind when the licensing code was removed.
 */
export function TrialBanner() {
  return null;
}
