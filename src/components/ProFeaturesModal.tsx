/**
 * Legacy Pro upsell modal. Renders nothing — every feature is free.
 * Call sites and app-store flag remain so we don't churn App.tsx wiring.
 */
export function ProFeaturesModal() {
  return null;
}
